import React from 'react';

export default function RaceListLoading() {
    //const router = useRouter();

    return(
        <div style={{height: '60vh', width:'100%', display: 'flex', justifyContent: 'center', alignItems: 'center', overflow: 'hidden'}}>
            <h3>Loading Content...</h3>
        </div>
    )
}